#include <iostream>
#include <stdio.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0); 
    cin.tie(0); cout.tie(0);
    
    cout<<"Hello ";
    printf("World \n");
    // cout<<"Is the order reversed ? Why isn't it";
}
